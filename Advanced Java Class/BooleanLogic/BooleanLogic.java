package BooleanLogic;

public class BooleanLogic {
    public static void main(String [] args) throws Exception{
        /*The goal of boolean logic is to simplify to something logically
        * equivalent. One way to do this is to replace with placeholder statements
        *
        * if (x>10 || (x<=10 && y!=5)) --> if( A || (!A && B))
        * you can simplify this to
        * if (A || B)
        *
        * With logic, to help verify iti is functioning as intended,
        * we can use truth tables
        * */
    }
}
